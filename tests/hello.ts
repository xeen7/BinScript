//const message: string = "Hello from BinScript!";
const user = {
    name: "samon",
    age: 25
};
let id = 5;
id *= (id + 5) * user.age;
console.log(`hello,${user.name},${user.age} , your id is : ${id}`);
