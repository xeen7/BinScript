[package]
name = "ownership-inference"
version = "0.1.0"
edition = "2021"

[dependencies]
hir = { version = "0.1.0", path = "../hir" }
mir = { path = "../mir" }
petgraph = { workspace = true }
tracing.workspace = true
